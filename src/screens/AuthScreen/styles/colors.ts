/**
 * Auth color tokens — backed by the shared app theme.
 * Prefer importing from `src/theme` in new code.
 */
import { colors } from '../../../theme';

export const AUTH_COLORS = {
  background: colors.background,
  gradientStart: colors.gradientStart,
  gradientMid: '#F3F0FF',
  gradientEnd: colors.gradientEnd,
  white: colors.white,
  primary: colors.primary,
  primaryDark: colors.primaryDark,
  primaryPurple: colors.primaryPurple,
  primaryPurpleDark: colors.primaryPurpleDark,
  primaryMid: colors.primaryMid,
  primaryLight: colors.primaryLight,
  primarySoft: colors.primarySoft,
  text: colors.text,
  textSecondary: colors.textSecondary,
  subText: colors.subText,
  muted: colors.muted,
  border: colors.border,
  borderFocus: colors.borderFocus,
  inputBg: colors.inputBg,
  googleBorder: colors.googleBorder,
  error: colors.error,
  success: colors.success,
  shadow: colors.shadow,
  overlay: colors.overlay,
};
